////  CCBNode.cpp//  UICodeGenerator////  Created by dansen on 13-4-9.//  Copyright (c) 2013年 bodhi. All rights reserved.//#include "CCBNode.h"CCBNode * node_new(std::string name, CCBNode * parent, int level){    CCBNode * node = new CCBNode();        node->name = name;    node->parent = parent;    node->property = new CCBNodeProperty();    if(parent){        parent->children.push_back(node);    }        node->level = level;    return node;}void node_release(CCBNode * node){    if(node){        for(int i = 0; i < node->children.size(); ++i){            node_release(node->children[i]);        }        delete node->property;        delete node;        node = 0;    }}